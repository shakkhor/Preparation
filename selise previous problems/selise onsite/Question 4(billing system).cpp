#include<bits/stdc++.h>
using namespace std;

int main()
{

    int limit;
    int n;
    scanf("%d",&n);
    int sh,sm;
    char ch;
    scanf("%d%c%d",&sh,&ch,&sm);

    printf("n=%d sh=%d ch=%c sm=%d",n,sh,ch,sm);

    //total_cost = ((7*60+14)*1)+((6*60)+14)*2+()+();
    int id;

    if(sh>=0 && sh<=7)
    {
        if(sm<=15)
        {
            id=1;
        }
        else if(sm>15)
        {
            assign =0;
        }

    }
    if(sh>=7 && sh<=13)
    {
        if(sh==13)
        {

        }
    }
    if(sh>=13 && sh<=22)
    {
        if(sh==22)
        {

        }
    }
    if(sh>=22 && sh<=23)
    {
        if(sh==23)
        {

        }
    }








    return 0;
}
